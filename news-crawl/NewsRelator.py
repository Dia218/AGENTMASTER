import importlib
errorHandler = importlib.import_module("news-error-handle.ErrorHandler")

newsCandidates = {}

def relateNews(newsNumber: int, nRelatedNews: int = 5):
    relatedNews = {}

    for news in newsCandidates:
        ...

    return relatedNews
    